services:
  web:
    build: .
    expose:
      - "3000"

  nginx:
    image: nginx:1.30
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - web
